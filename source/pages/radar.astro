---
import BaseLayout from '../layouts/BaseLayout.astro'
import EditorialRadar from '../components/EditorialRadar.astro'
import { EDITORIAL_RADAR } from '../data/generated/editorial-radar'
import { SITE } from '../config'

const topicCount = EDITORIAL_RADAR.topics.length

const collectionSchema = {
  '@context': 'https://schema.org',
  '@type': 'CollectionPage',
  name: 'Radar editorial — Temas por explorar',
  description: 'Análisis automático de huecos de contenido sobre inteligencia artificial generativa. Temas detectados por el pipeline editorial de Identidad Artificial a partir del corpus publicado.',
  url: `${SITE.url}/radar/`,
  inLanguage: 'es',
  isAccessibleForFree: true,
  author: {
    '@type': 'Person',
    name: SITE.author,
    url: `${SITE.url}/sobre/`,
  },
  publisher: {
    '@type': 'Organization',
    name: SITE.name,
    url: SITE.url,
  },
  mainEntity: {
    '@type': 'ItemList',
    name: 'Temas detectados por el radar editorial',
    numberOfItems: topicCount,
    itemListElement: EDITORIAL_RADAR.topics.map((t, i) => ({
      '@type': 'ListItem',
      position: i + 1,
      name: t.topic,
      description: t.intent,
    })),
  },
}
---

<BaseLayout
  title="Radar editorial — Temas por explorar"
  description="Análisis automático de huecos de contenido sobre IA generativa. El pipeline detecta temas sin cobertura en el corpus publicado y los prioriza por señales editoriales."
  image="/og/radar.png"
  jsonLd={collectionSchema}
>
  <section class="page-hero">
    <nav aria-label="Ruta de navegación"><a href="/">Inicio</a> / <span aria-current="page">Radar editorial</span></nav>
    <p class="page-eyebrow">Radar editorial</p>
    <h1>Temas por explorar</h1>
    <p class="page-subtitle">
      Propuestas para futuros artículos a partir de lo que ya hemos publicado. No son noticias en directo ni un calendario de publicación comprometido.
    </p>
  </section>

  <div class="page-body">
    <EditorialRadar radar={EDITORIAL_RADAR} />
    <section class="radar-meta">
      <h2>Cómo se eligen las propuestas</h2>
      <p>
        El radar analiza el corpus de <a href="/">{SITE.name}</a> en cada build y detecta
        <strong>{topicCount} huecos de contenido</strong> — temas con señales en los artículos
        existentes pero sin pieza dedicada. Cada entrada incluye la intención de búsqueda estimada,
        las etiquetas relacionadas y los artículos que generaron la señal.
        Los datos se regeneran en cada deploy sin llamadas a APIs externas.
        Más detalles en <a href="/como-funciona/">cómo funciona el sitio</a>.
      </p>
    </section>


  </div>
</BaseLayout>

<style>
.page-hero {
  max-width: 1152px;
  margin: 0 auto;
  padding: 32px 20px 24px;
}

.page-eyebrow {
  color: #34D399;
  font-size: 11px;
  font-weight: 800;
  letter-spacing: .12em;
  text-transform: uppercase;
  margin: 0 0 10px;
}

h1 {
  color: #E8E8F0;
  font-size: clamp(30px, 5vw, 48px);
  font-weight: 900;
  line-height: 1.15;
  margin-top: 10px;
}

.page-subtitle {
  display: block;
  max-width: 680px;
  color: #C8C8E0;
  font-size: 16px;
  line-height: 1.7;
  margin-top: 16px;
}

.page-body {
  max-width: 1152px;
  margin: 0 auto;
  padding: 16px 20px 48px;
}

.radar-meta {
  max-width: 680px;
  margin: 0 auto 32px;
}

.radar-meta h2 {
  font-size: 11px;
  font-weight: 600;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  color: #C8C8E0;
  padding-bottom: 12px;
  border-bottom: 1px solid #2A2A40;
  margin-bottom: 16px;
  margin-top: 0;
}

.radar-meta p {
  color: #C8C8E0;
  font-size: 14px;
  line-height: 1.7;
  margin: 0;
}

.radar-meta strong {
  color: #E8E8F0;
}

.radar-meta a {
  color: #34D399;
  text-decoration: none;
  border-bottom: 1px solid transparent;
  transition: border-color 0.15s;
}
.radar-meta a:hover {
  border-bottom-color: #34D399;
}
nav { margin-bottom: 24px; color: #C8C8E0; }
nav a { color: #A78BFA; }
.radar-meta { margin-top: 48px; }
</style>
